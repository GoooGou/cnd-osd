# 2026-09-10

## cnd-osd 项目：初始化 Git 并推送 GitHub

- 项目：Figma Make 导出的 React 19 + Vite 8 + Tailwind v4 应用（Design OSD Interface）。
- 仓库：https://github.com/GoooGou/cnd-osd （public，已存在，owner id 162654668）。
- 本机 git 未配置身份，采用 GitHub noreply 邮箱提交：
  `GoooGou <162654668+GoooGou@users.noreply.github.com>`（避免 .local 域名邮箱导致 Vercel 部署被拦）。
- 提交记录：first commit → chore: pin node engine → chore: ignore .DS_Store。

## 环境事实（重要）

- 本机 git 走 socks5 代理：`~/.gitconfig` 中 `[https] proxy = socks5://127.0.0.1:7897`，push 正常。
- 本机**无 gh、无 vercel CLI**。
- `npm i -g <pkg>` 被环境 broker 拦截（`CODEBUDDY_BROKER_DENY`，mkdir 无 runtime file rule），
  **vercel CLI 装不上**；但 `pnpm` 全局安装成功了。
- pnpm 12.3.4 已装在 `/Users/wangdan/.workbuddy/binaries/node/versions/22.22.2-2/bin/pnpm`。
- `pnpm install && pnpm build` 在沙箱下会报 exit!=0（沙箱拦 pnpm 临时文件 unlink），
  但**构建产物 dist 实际生成成功**，不是构建失败。

## Vercel 部署

- 未拿到 Vercel 凭据（无 token、无 CLI、无 GitHub App 授权），无法自动部署。
- 已做的准备：package.json 加 `"engines": {"node": ">=22.12"}`（Vite 8 要求 Node 20.19+/22.12+，
  防 Vercel 用旧 Node 构建失败）；`.gitignore` 加 `.DS_Store`。
- Vercel 自动识别 Vite + pnpm-lock.yaml(lockfileVersion 9.0)，无需 vercel.json。
- `.figma/make/site.json` 已入库，vite.config.ts 依赖它，Vercel 构建不会缺文件（.gitignore 只排除了 `/.figma/design-context/`）。
- 部署入口：https://vercel.com/new/clone?repository-url=https://github.com/GoooGou/cnd-osd
